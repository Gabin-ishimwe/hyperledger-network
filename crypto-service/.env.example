# Crypto Service Environment Configuration

# Service port
PORT=8080

# Path to crypto materials (relative to service directory)
# Default: ../organizations (points to the network's organizations directory)
CRYPTO_BASE_PATH=../organizations

# Node environment
NODE_ENV=production
